diff --git a/tests/harness/test_action_availability.py b/tests/harness/test_action_availability.py
index 5415a03..840e6bd 100644
--- a/tests/harness/test_action_availability.py
+++ b/tests/harness/test_action_availability.py
@@ -216,7 +216,7 @@ def test_run_env_routes_actor_reset_through_attempt_boundary():
                     available_actions=[1],
                 )
             return SimpleNamespace(
-                frame=[[[1]]],
+                frame=[[[9]], [[1]]],
                 levels_completed=1,
                 state=GameState.WIN,
                 available_actions=[1],
@@ -258,7 +258,7 @@ def test_run_env_routes_actor_reset_through_attempt_boundary():
 
     arc = FakeArcade()
     agent = ResetAgent()
-    rec = run_env(agent, arc, "reset-game", [3], keep_trace=True)
+    rec = run_env(agent, arc, "reset-game", [3], keep_trace=True, keep_frames=True)
 
     assert agent.reset_available == [GameAction.ACTION1]
     assert agent.reset_notes == 1
@@ -266,3 +266,7 @@ def test_run_env_routes_actor_reset_through_attempt_boundary():
     assert rec.resets == 1
     assert rec.total_actions == 2
     assert rec.reset_actions_counted is True
+    assert rec.trace[1]["pre_action_frame"] == [[0]]
+    assert rec.trace[1]["returned_frames"] == [[[9]], [[1]]]
+    assert rec.trace[1]["available_actions_before"] == ["ACTION1"]
+    assert rec.trace[1]["frame"] == [[1]]
diff --git a/tycho/harness/harness.py b/tycho/harness/harness.py
index c862ff6..d011726 100644
--- a/tycho/harness/harness.py
+++ b/tycho/harness/harness.py
@@ -163,6 +163,11 @@ class TraceStep:
     frame_key: int  # hash of the resulting grid (loop / revisit detection)
     frame: Optional[list] = None  # resulting HxW grid — only when keep_frames (viz)
     reasoning: Optional[dict] = None  # agent's per-step internal state, if it attached any
+    # Lossless observation envelope for external causal-trace audit. These fields are logging-only
+    # and populated only with keep_frames; they do not alter agent inputs or engine controls.
+    pre_action_frame: Optional[list] = None
+    returned_frames: Optional[list] = None
+    available_actions_before: Optional[list[str]] = None
 
 
 @dataclass
@@ -515,6 +520,22 @@ def run_env(
                     frame_key=new_key,
                     frame=trace_grid,
                     reasoning=step_reasoning,
+                    pre_action_frame=(
+                        (
+                            _grid_to_first(frame.frame)
+                            or _grid_to_list(frame.frame)
+                        )
+                        if keep_frames and state == GameState.GAME_OVER
+                        else (_grid_to_list(frame.frame) if keep_frames else None)
+                    ),
+                    returned_frames=(
+                        _frame_sequence_to_list(next_frame.frame) if keep_frames else None
+                    ),
+                    available_actions_before=(
+                        [a.name for a in normalize_game_actions(frame.available_actions)]
+                        if keep_frames
+                        else None
+                    ),
                 )
             )
         prev_key = new_key
