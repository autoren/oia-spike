# Track A 005b: intervention-triggered revision

This successor slice asks whether the opened Track A 005a `ACTION6` transition
can support a new fixed executable frontier and a prospective OIA-selected next
action before any step-2 payload is opened.

The exact reset raster, both observed transient-animation variants, the common
settled P1 raster, generation prompts, parser, candidate contract, isolated
calibration runner, post-P1 action language, determinism matrix, adapter, OIA
invocation, baselines, and allowed verdicts are frozen here before revision
inference.

The two transient variants are deliberately preserved. Their five source
records have a 3/2 split and agree exactly only at the settled P1 frame. Because
the candidate action interface exposes action names but not recorded click
coordinates, neither transient sequence is treated as a deterministic
calibration target. Exact P0 replay and exact settled P1/RUNNING replay after
`ACTION6` remain mandatory.

Calibration is not prediction. A later successful result could establish only
a bounded opened Track A revision cycle on this public recording family. It
cannot establish de novo discovery, sealed transfer, general decision value,
or a Track B claim.
