# Agent 02 attempt 02 — imported acquisition evidence

This directory contains compact evidence copied or derived from the exact Agent 02 acquisition at source commit `f5a88d41fb6c4abf34bbd5210ceedd4584ac3416` and independently verified in `oia-spike`.

- `attempt_01_zero_selection.json` preserves the first locator mismatch.
- `ACQUISITION_PATH_ALIAS_ERRATUM.md` documents the semantic-label to archive-path correction.
- `checkpoint_enumeration_rule.frozen.json` preserves the source/provenance-based candidate order fixed before checkpoint behavior inspection.
- `ACQUISITION_SUMMARY.json`, `RUN_LABEL_MAPPING.json`, and `ARCHIVE_RUN_ROOTS.json` are byte-identical compact files from corrected attempt 02.
- `LOCAL_IMPORT_VALIDATION.json` records the independent outer ZIP, inner package, payload, selected-file-manifest, safe-entry, and read-only checks.
- `SOURCE_PROVENANCE.json` identifies the remote commits, workflow run, artifact, omitted bulk manifests, and claim boundary.

No file in this directory is an admitted candidate or evidence of candidate execution, replay, determinism, behavioral divergence, OIA separability, ontology revision, or decision value.
